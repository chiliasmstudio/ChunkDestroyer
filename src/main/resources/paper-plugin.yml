name: ChunkDestroyer
version: '1.0.1'
main: com.chiliasmstudio.ChunkDestroyer.ChunkDestroyer
api-version: '1.21'
folia-supported: true
dependencies:
  server:
    Residence:
      load: BEFORE
      required: true
      join-classpath: true
    Vault:
      load: BEFORE
      required: true
      join-classpath: true